В App.js:

1. в Header прокинути пропс count:

<Header count={ подумай, як обчислити кількість продуктів в корзині} />

Підказка: корзина - це cartItems - масив продуктів: 
скільки елементів в масиві, стільки і різних продуктів в корзині



В Header.js

2. "відловити" цей пропс count

3.В реакт-бутстрап є компонент Badge - в ньому і напишемо {count}
після напису Корзина (в тому ж компоненті Header.js)

4.* Додай умову, щоб count відмальовувався лише тоді, коли він не рівен 0

(як ми робили з написом "Корзина порожня")


